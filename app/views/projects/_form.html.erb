      <%= simple_form_for @project do |f| %>
        <%= f.input :name, placeholder:"Expo 2020", label:"Choose a project name" %>
        <%= f.input :stage, placeholder:"Design", label:"Detail the project stage"%>
        <%= f.button :submit, "Add", class:"btn btn-primary" %>

      <% end %>
